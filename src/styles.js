const segmentStyle = {
  boxShadow: "3px 3px 5px 0px rgba(0,0,0,0.5)",
  border: "1px solid GhostWhite",
  margin: "0.5em 0",
  backgroundColor: "#f6f6f5"
};

const popupStyle = {
  backgroundColor: "cornsilk"
};

const labelStyle = {
  margin: "0.2em",
  maxWidth: "150px",
  color: null
};

const linkStyle = {
  display: "block",
  fontFamily: "IBM Plex Mono",
  fontSize: "0.8em"
};

const messageStyle = {
  position: "absolute",
  top: 0,
  width: "100%",
  zIndex: 2
};

export { segmentStyle, messageStyle, popupStyle, labelStyle, linkStyle };
